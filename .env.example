# Dev (SQLite, async):
DATABASE_URL=sqlite+aiosqlite:///./budgy.db

# Prod (PostgreSQL, async) - just swap the URL, no code changes needed:
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/budgy

SECRET_KEY=change-this-to-a-long-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=60
